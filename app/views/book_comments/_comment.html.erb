<tbody>
  <tr>
    <% books.book_comments.each do |comment| %>
    <td>
        <%= attachment_image_tag comment.user, :profile_image, :fill, 40, 40, format:'jpeg', fallback:"noimage.jpg" %>
        </br>
        <%= comment.user.name %>
    </td>
    <td>
       <%= comment.comment %>
    </td>
    <td>
        <% if comment.user == current_user %>
         <%= link_to "Destroy", book_book_comments_path(comment.book,comment), method: :delete, class:"btn btn-sm btn-danger" %>
        <% end %>
    </td>
  </tr>
  <% end %>
</tbody>